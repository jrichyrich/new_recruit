interface:
  display_name: "RosterPilot"
  short_description: "Build and validate Warhammer 40,000 army rosters"
  default_prompt: "Use $rosterpilot to inspect, build, modify, validate, explain, or export the requested Warhammer 40,000 roster, then perform only the New Recruit handoff or local Tessera analysis the user explicitly requests."
